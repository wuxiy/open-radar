# Open Radar 架构与项目结构

> 修订日期：2026-09-05。
> 状态：架构决策已确认，V0.1、V0.2 和 V0.2.1 的本地核心已实现；真实 GitHub Issue/PR 写入、受控仓库 E2E、分支保护和签名仍属于外部集成门禁。
> 本文件不代表已修改仓库可见性、权限、密钥或远端设置。

本文件汇总架构评审中确认的决定，作为后续实现依据。问答索引、文档验证记录和产品验收清单见 [任务记录](tasks/todo.md)。后续明确修订的决定优先于早期方案。

<a id="purpose"></a>

## 1. 产品定位与范围

Open Radar 是个人技术决策雷达，收录少量值得持续关注的开源项目，保存研究依据，并支持选型、采用和源码借鉴。成功指标是促成的研究与工程决策，不是收录数量。

正式项目库称为 Catalog。候选项目只存在于 Issue 和审核流程中，人工合并收录 PR 后才进入 Catalog、开始保存持续观测。活跃关注指 `tracking` 不为 `off` 的项目，其数量受可配置容量限制；停止关注时关闭采集，保留历史知识。容量数值和运行预算须在上线前配置，不假定 Git 可以无限承载历史。

V0.1 只接受至少包含一个公开 GitHub 仓库的开源项目：

- `Project URL` 必须能解析为公开 GitHub 仓库。
- X、HN、Reddit、博客等链接只作为发现来源保存，不能独立形成项目记录。
- 私有仓库、GitLab、论文、纯产品和纯网站暂不支持，返回 `unsupported-source`。
- 自动 Scout 延后；将来发现的项目也只能进入候选池。

目标部署采用私有权威仓库。个人笔记、采用决定、自研项目上下文、Issue 和研究 PR 不直接公开。以后如需发布，只导出经过筛选和脱敏的派生视图。引擎可另行开源，但本次不拆仓库，也不改变当前仓库设置。

<a id="mvp"></a>

## 2. V0.1 交付边界

首版交付这一条完整流程：

```text
授权提交 GitHub URL
  → 验证仓库身份、去重、读取基础事实
  → 创建或更新收录 PR
  → 人工审核并合并
  → 按 tracking 定时观测
  → 自动合并机器数据 PR
  → 更新 README
```

### 2.1 必须交付

- Issue 入口、执行授权及明确的不支持来源提示。
- 稳定项目 ID、GitHub 仓库身份解析和幂等去重。
- 一个候选对应一个收录事务和持续更新的 PR。
- 基础项目 Schema、受控分类标签、三维状态和人工审核。
- Git 内按月追加的规范化观测、纠正记录及紧凑运行清单。
- 分级调度、失败重试、多读单写和机器数据发布通道。
- 确定性 README 生成、数据验证、权限隔离及验收测试。

### 2.2 可选增强

LLM 可建议分类、标签和简短摘要，但不进入必经路径。未配置 LLM、请求失败或输出无效时，仍应创建基础收录 PR，由人工补齐必要字段。启用增强时，必须同时具备提案 Schema、证据和权限隔离。

### 2.3 后续能力

深度研究、关系图谱和公开发布仍不属于首版。V0.2 已在本地实现确定性 Change Intelligence、评分和历史报告；本文保留其数据与权限边界，不要求 V0.1 提前实现完整系统。

Web UI、语义搜索、RAG、MCP Server、多来源采集和动态源码验证另行排期。首版不引入数据库、对象存储、向量库、消息队列、独立后台、用户系统或复杂多 Agent 框架。

<a id="data-layers"></a>

## 3. 数据权威与所有权

三层数据分别承担不同职责：

| 层 | 保存内容 | 写入与审核 | 能否重建 |
|---|---|---|---|
| 人工知识 | 项目身份、分类、状态、笔记、已接受分析、上下文和关系 | Agent 只能提案，人工审核后合并 | 不能从 API 重建 |
| 外部观测 | 带来源、时间与版本的规范化事实及追加纠正 | 确定性采集，可经严格校验自动合并 | 过去的观测不能用当前 API 替代 |
| 派生视图 | 当前指标、README、计算分数、搜索索引、过期提示 | 可信生成器从固定输入生成 | 可以重建 |

`data/projects/*.yaml` 不再保存随采集刷新的 `metrics`、外部归档状态或综合评分。人工修订并接受的分析属于人工知识；Agent 重跑只能提交新提案和差异，不能直接覆盖。外部变化产生的 `stale` 提示是派生状态，不反写人工结论。

事实来自确定性数据源。GitHub 返回的描述、主题等只是外部声明，不等同于已审核结论。缺失值应保留为未知，并附原因；采集失败不能转换成零 Stars、无 License 或已停止维护。健康度、成熟度、创新性等判断需规则或研究证据，不能伪装成 API 原始事实。

后续发布的研究与报告虽可由 Agent 起草，人工接受后也是历史知识。相应的来源、生成版本和接受记录一并保留。

<a id="project-model"></a>

## 4. 项目、仓库与身份

一条 Catalog 记录表示一个逻辑项目，可以关联多个 GitHub 仓库。研究、用途决定和个人笔记归属于项目；Stars、Forks、Commit 和 Release 归属于具体仓库。

### 4.1 本地主键

项目使用创建后不可变的可读 slug，例如 `radar-demo`：

- 文件名与跨文件引用使用 `id`，改名或迁移组织不改变主键。
- `display_name` 可更新，`aliases` 保存历史名称或检索别名。
- 同名冲突在首次收录时人工加限定词，不自动复用已有 ID。
- GitHub URL、owner、repo 仅用于定位，外部身份由 `provider + repository_id` 确定。
- 仓库迁移后重新解析位置，不能按变化的 URL 创建重复项目。此行为须纳入真实 API 集成验证。

项目 YAML 内的 owner 和 repo 是最近一次人工接受的位置记录。采集依赖稳定仓库身份，最新位置可从观测展示；位置变化通过提案更新 YAML。

### 4.2 多仓库规则

V0.1 项目至少关联一个公开 GitHub 仓库，并且恰有一个 `primary`。附属仓库可标记为 `sdk`、`docs` 等受控角色。

默认列表只展示主仓库指标，并标明归属；不能把多个仓库的 Stars 隐式相加。将来如计算项目健康度，必须使用显式、带版本的聚合规则。修改主仓库角色由人工审核，历史观测仍保留原 repository ID。

### 4.3 重复、合并与拆分

相同 repository ID 是确定性重复，候选可自动标记并关闭，同时链接到已存在的项目或开放事务。不同仓库仅因名称、组织或主页相似，只能标记为 `possible-duplicate`，由人决定关联、独立收录或拒绝。

逻辑项目的合并或拆分必须人工批准，通过显式迁移更新当前引用，并保存旧 ID 到新 ID 的重定向。旧 ID 不得重新分配，历史观测不搬运、不改写；读取时通过重定向解析旧引用。Fork、替代品与附属仓库不能仅由名称推断。

<a id="state-taxonomy"></a>

## 5. 状态与分类

### 5.1 三维项目状态

最终模型替代早期单一 `status` 以及 `research_stage + disposition` 方案。

| 字段 | 允许值 | 表达的含义 |
|---|---|---|
| `research_stage` | `watching`、`researching`、`evaluated` | 研究进度 |
| `decision` | `undecided`、`adopt`、`reference`、`reject` | 使用或借鉴决定 |
| `tracking` | `daily`、`weekly`、`monthly`、`off` | 采集频率 |

三个维度均由人工维护，允许已采用的项目继续研究。界面的归档操作设置 `tracking: off`，并记录停止时间和原因，不删除项目，也不抹去 `decision`。重新开启采集经人工修改，历史操作仍可从 Git 追踪。

Issue 的 `pending`、`inbox`、`agent-processing`、`needs-review`、`accepted`、`rejected`、`duplicate`、`possible-duplicate`、`error` 属于收录流程，不写成项目状态。已收录项目的 `decision: reject` 表示后续评估决定，区别于拒绝收录的 Issue。

### 5.2 分类词表

每个项目只设一个 `primary_category`，细分能力通过多值 `tags` 表达。文件统一放在 `data/projects/<id>.yaml`，分类变化不移动文件。

分类和标签使用受控词表，支持 alias 归一化。Agent 只能选择已有词条或在提案中输出 `proposed_tags`；未获批准的标签不得进入正式数据和展示。新增词条需说明定义、适用边界及与近义词的区别，重命名通过映射和迁移完成。原稿中 `ai / agent / coding-agent` 并列为一级分类的示例不再适用。

### 5.3 基础项目示例

以下项目、仓库 ID、URL 和内容均为虚构示例，未经外部采集。示例是目标契约，尚无实现的 Schema 可供验证。

```yaml
schema_version: 1
id: radar-demo
display_name: Radar Demo
aliases: []

repositories:
  - provider: github
    repository_id: 100000001
    owner: example-org
    repo: radar-demo
    role: primary

primary_category: devtools
tags:
  - automation

discovery_sources:
  - type: manual
    url: https://github.com/example-org/radar-demo
    discovered_at: "2026-09-03T00:00:00Z"

research_stage: watching
decision: undecided
tracking: weekly
personal_notes: ""
```

收录 PR 可包含未经合并的分类或摘要建议，但正式合并前必须满足基础 Schema。无摘要时可以省略已审核分析字段；展示层可使用标明来源的外部描述，不能编造默认分析文案。

<a id="observations"></a>

## 6. Git 内的观测日志

V0.1 的长期数据自包含于 Git。外部观测保存为 `data/observations/github/YYYY-MM.jsonl`，每行一个规范化记录，不保存完整 API 响应或逐项目逐日快照文件。

采集时间、记录时间使用 UTC。按 `recorded_at` 所在月份追加，月份结束后文件只读。迟到结果和历史纠正写入当前月份，保留原观测时刻，不能回写已关闭分区。

### 6.1 记录要求

每条观测至少包含：

- 唯一 `event_id`、稳定采样键 `collection_key` 和 `run_id`。
- 采集时的项目 ID、provider、repository ID。
- 计划采样时刻、实际观测时刻、写入时刻。
- `schema_version`、采集器版本、来源和指标。
- 缺失或不支持字段的原因，不能把未知值当成有效零值。

采样键标识一次计划采样，重试沿用原键。不同时间的真实采样不是重复；同一采样键已有有效记录时，重放不再追加新观测。出现不同结果时必须显式处理冲突或追加纠正，不能静默写两份。

以下 JSONL 为虚构记录，时间和指标只用于说明结构：

```jsonl
{"schema_version":1,"record_type":"observation","event_id":"obs-demo-001","collection_key":"github:100000001:2026-09-03T00:00:00Z","run_id":"run-demo-001","project_id":"radar-demo","provider":"github","repository_id":100000001,"scheduled_at":"2026-09-03T00:00:00Z","observed_at":"2026-09-03T00:02:00Z","recorded_at":"2026-09-03T00:03:00Z","collector_version":"collector-v1","source":"github-api","metrics":{"stars":42,"forks":3},"facts":{"archived":false,"license_spdx":null},"unavailable":{"license_spdx":"not_reported"}}
```

### 6.2 纠正与失效

已合并的错误记录不得直接修改、删除或重写 Git 历史。追加纠正记录，包含新的 `event_id`、`supersedes`、`correction_reason` 和采集器版本；必要时追加失效记录。

纠正必须指向已存在的记录，保留同一仓库和被纠正的采样身份。校验器拒绝悬空引用、循环纠正和未解决的分支冲突。派生视图采用最终有效记录，并能追溯原值。由采集器 Bug 引发的修复还需标出受影响批次。

当前值按有效观测时刻选取，而非按最后写入行选取，避免较晚写入的历史纠正覆盖真正的新数据。历史样本不足时不计算对应窗口增长率。

<a id="contracts"></a>

## 7. Schema 与引用契约

YAML 是人工编辑格式，JSONL 是追加日志格式；二者都通过版本化 Schema 校验。共享约束包括 `schema_version`、未知字段拒绝、日期格式、枚举、数值范围、稳定 ID 和跨文件引用。

| 契约 | 用途 | 实施时点 |
|---|---|---|
| `project.v1.json` | 项目、仓库角色、人工状态 | V0.1 必须 |
| `observation.v1.json` | 观测、纠正与失效记录 | V0.1 必须 |
| `run-manifest.v1.json` | 紧凑运行清单及后续运行事件 | V0.1 必须 |
| `taxonomy.v1.json` | 分类、标签、别名和角色 | V0.1 必须 |
| `research-proposal.v1.json` | 有证据的分析提案 | 启用可选 LLM 前 |
| `change-event.v1.json` | 确定性变化事件 | Change Intelligence 实施前 |
| `context.v1.json`、`relation.v1.json` | 私人上下文与关系边 | 对应后续功能实施前 |

名称为计划文件名，Schema 的正式字段定义在实现前冻结。全局唯一性、主仓库数量、taxonomy 合法性、重定向、观测纠正链等由跨文件校验补充，不能仅靠单条 JSON Schema。

Schema 升级使用显式迁移和兼容读取逻辑。当前人工数据可通过人工审核的迁移 PR 更新；旧观测日志保留原版本，通过读取适配或新增纠正演进，不原地批量改写。冻结 Schema 指冻结一个版本的契约，不表示以后不能发布新版本。

<a id="admission"></a>

## 8. 收录事务与降级

### 8.1 入口授权

Issue 表单只要求 Project URL，Source URL 和 Comment 可选。受信用户提交后可直接排队；其他提交停在 `pending`，由维护者添加 `approved-for-processing` 才执行昂贵自动化。

每次触发都校验操作者和授权来源，不能信任 Issue 正文中的自我声明。评论命令、标签事件、外部链接和 Fork PR 遵守同一规则。即使目标仓库私有，也保留该入口约束。

按用户、仓库和时间窗口设置速率与预算限制。达到预算上限时暂停新分析并记录原因。预算耗尽和权限不足不能解释为允许绕过控制。

### 8.2 一候选、一事务、一收录 PR

收录任务使用接收仓库身份和 Issue 编号构成幂等键；仓库重复检测另用 GitHub repository ID。一次任务依次完成授权、身份解析、去重、基础事实获取、提案生成和 PR 发布。

重复事件或重试更新原分支与原 PR，不重新创建项目。相同逻辑项目只能存在一个开放收录 PR；明确仓库重复由单写入发布阶段再次检查，语义相似但无法判断的候选等待人工确认。

Issue 标签只是展示状态。真实结果根据事务记录、PR 状态和合并结果推导。事务记录保留在 PR 元数据及紧凑运行清单中，不另建长期未审核内容目录。发现标签与真实结果不一致时修正标签，不能据此重复执行已完成操作。

PR 合并后才创建正式项目记录并开始观测。候选阶段获取的基础事实只用于审核，临时响应不写入正式观测日志。合并事件漏投时，通过已合并 PR 和目录状态对账恢复。失败使用结构化错误代码并支持安全重试。

### 8.3 收录不等待研究

Admission 与 Research 是独立流程。基础身份、主分类、跟踪级别及必需 Schema 经人工确认后即可收录，不要求完成深度研究或评分。

LLM 不可用时，提案状态可标记 `pending`，不向严格枚举字段塞入占位值。人工在合并前补齐必要分类；摘要等非必要分析可省略。LLM 恢复后的重试只更新尚未接受的提案，不能重复收录或覆盖已审核知识。

<a id="collection"></a>

## 9. 调度与多读单写

定时采集仅依据人工接受的 `tracking` 执行：

| tracking | 频率 | 默认建议 |
|---|---|---|
| `daily` | 每日 | 正在研究或已经采用 |
| `weekly` | 每周 | 持续观察 |
| `monthly` | 每月 | 已评估或仅作参考 |
| `off` | 停止 | 主动归档或暂停 |

默认建议仅帮助人工首次选择。修改 `research_stage` 或 `decision` 不得悄悄改变 `tracking`，Agent 更不能自动联动状态。Inbox 不参加定时采集，手动刷新也进入统一调度通道。

调度器维护可重建的 `next_collection_at`、重试进度和游标，其依据是配置、有效观测和运行结果；它们不属于人工项目 YAML。采集失败采用有界退避，不把失败时间当成成功采样。将来可接入 Release 等事件提前触发，轮询仍提供补偿。

各仓库可以并行读取，写入通过单一汇总器：

1. 采集任务产生隔离的临时结果。
2. 汇总器验证、去重、按稳定顺序追加并生成运行清单。
3. 发布器在同一观测分区的并发锁下更新一个机器数据 PR。
4. CI 通过后合并，下一批再基于最新主分支写入。

锁和提交前去重覆盖定时、手动以及重试入口；仅限制并行启动不能代替幂等。若上一批尚未合并，新结果须排队或汇入同一待合并批次，不能并行修改同一月文件。

单个仓库失败不阻塞其他有效记录，但必须在清单中标明部分成功。系统不以伪造观测补齐失败项，也不因自己生成 README 或清单的提交再次触发采集。

<a id="publishing"></a>

## 10. 双通道发布

| 通道 | 允许内容 | 合并方式 |
|---|---|---|
| 机器数据 PR | 规范化观测、合法纠正、紧凑运行清单、可信生成器产出的 README | 必需检查通过后可自动合并 |
| 人工知识 PR | 项目身份与状态、taxonomy、分析、研究、关系、上下文和叙述性报告 | 人工审核 |

机器数据 PR 也称 observation-only PR，指没有新增人工判断，并非字面上只能改一个观测文件。审核分类以实际差异为准，不能信任标签或 PR 标题。混合两类内容的 PR 取消自动合并资格。

机器通道校验允许路径、追加语义、版本、引用、生成结果和来源。配置、Schema、工作流及人工知识路径均不得通过此通道自动修改。README 必须由主分支的可信模板和生成器重建，不能把 Artifact 中的任意 Markdown 当作可信生成物。

自动合并使用可审计的独立 Bot 身份，并依赖不能被该通道绕过的必需检查。路径检查本身不等于凭据的路径级权限；上线前必须实测保护规则和令牌权限。未满足隔离条件时关闭自动合并，不通过直接写主分支来绕过。

<a id="security"></a>

## 11. 执行权限与不可信输入

读取仓库内容不包含执行第三方程序的权限。README、Issue、源码、依赖清单以及外部 Agent 指令文件均是不可信数据。

### 11.1 三段式执行

| 阶段 | 输入与输出 | 凭据和执行边界 |
|---|---|---|
| Collector | 读取公开元数据和允许的内容，产出规范化事实 | 只读权限，无仓库写令牌、无 LLM 密钥 |
| Analyzer | 在启用时读取经过筛选的内容，产出结构化提案 | 受限 LLM 凭据，无仓库写权限，不给模型环境或任意命令访问能力 |
| Publisher | 验证产物，使用可信代码生成数据并创建 PR | 最小写权限，无 LLM 凭据，不解释或执行外部内容 |

不同阶段使用隔离工作流或作业环境，不共享可执行工作目录。Publisher 只运行受信分支上的代码，不检出或执行提案分支中的代码。只传递数据，禁止将序列化对象、路径或文本拼成待执行命令。

阶段产物带内容摘要及可验证的来源签名，绑定受信工作流、运行 ID、输入提交和目标。接收方验证来源、Schema、目标路径和有效期，拒绝伪造、越界或过期产物。摘要不能代替签名，签名也不能证明内容正确。签名或来源证明机制的具体实现须在上线前验证。

### 11.2 只读研究

V0.1 不执行外部仓库的安装、构建、测试、Git hooks 或指令文件，也不跟随文本中的工具调用要求。基础收录仅需 API 读取；后续源码研究可以在一次性环境中获取指定提交并只读分析。

分析环境的模型输入不包含密钥，出站访问按所需服务限制。将来确需动态验证时，单独设计无密钥、无网络、资源受限的沙箱阶段，并重新批准范围，不能沿用只读研究授权。

<a id="runs"></a>

## 12. 运行清单与恢复

每次自动化运行生成紧凑清单，长期追加到 `data/runs/YYYY-MM.jsonl`，保存：

- `run_id`、触发原因、输入提交、开始和结束时间。
- 采集器、Schema、Prompt、评分及生成器版本；未启用项显式注明不适用。
- 输入项目数，以及成功、失败、跳过和部分成功结果。
- 观测、变化事件、提案、关联 PR 标识和 Actions 运行链接。
- 结构化错误码及可获得的成本摘要，不以未知成本冒充零消费。

完整日志、API 响应和未审核模型草稿留在有保留期的临时 Artifact 中，不复制进长期清单，不保存密钥或大段私人上下文。

运行完成之后才知道的合并状态作为后续事件追加，不回填旧清单。运行 ID 不依赖包含自身记录的未来提交 SHA，避免自引用。所有长期日志写入都经过统一汇总通道，恢复过程以已发布事实对账。

未审核提案只存在于 PR 分支，拒绝后由关闭的 PR 保留审核记录，主分支不设置长期 `proposals/`。有长期价值的拒绝理由由人提炼为笔记或规则。这里的主分支限制针对待审知识内容，不排斥不含草稿的运行清单。

<a id="derived"></a>

## 13. README 与历史出版物

README 提交进 Git，作为 Catalog 的当前浏览入口。默认可展示最近收录、主分类、三维状态、主仓库指标和数据更新时间；尚未计算的评分或没有证据的摘要不展示为已完成结果。

生成器以固定数据、模板、版本和明确截止时间为输入，稳定排序，不隐式调用当前时间或网络。CI 重新生成后必须无差异。人工修改应回到数据源，不能直接编辑生成区域。搜索索引、缓存和中间计算表不进入 Git，可在构建时重建。

项目、分类或模板变更的人工 PR 同步运行生成器更新 README，仍需人工审核；机器数据 PR 则基于已接受知识生成。刚收录或停止采集的项目也能展示，无观测时明确标注暂无数据，不等待首个采样才出现。

周报和月报属于后续能力：叙述性内容先经 PR 审核，发布后固定为当时的判断。每份报告记录观测截止时间、项目集、输入版本、规则和 Prompt 版本。新数据不得静默重算过去报告；发现错误时提交说明明确的更正。README 与搜索索引继续反映当前状态。

未来公开视图从私有知识库中显式选择字段，不直接镜像数据、Issue、PR、日志或个人上下文。对外发布需另行授权。

<a id="future-analysis"></a>

## 14. 后续分析契约

本节均为后续能力，只有 V0.1 的可选简短分析需要提前遵循相应提案约束。

### 14.1 事件驱动的 Change Intelligence

先用确定性规则比较观测，生成标准化 `change_event`，再由达到阈值的事件触发分析。事件具备稳定指纹、规则版本和前后证据，重放不得重复产生分析提案。

Release、License、归档状态、提交变化等需按数据源的实际可获得性检测；维护者变动、重大架构变化等缺乏直接事实时，不作确定性结论。阈值触发可包含重大版本、License 改变、活跃度异常和核心文档变化。Agent 只分析选定变化，不定期全量重写所有项目。

变化结论和过期提示不直接改变人工状态，继续通过提案审核。

### 14.2 研究证据

研究由人工触发或已授权的研究策略触发，不阻塞收录。源码研究记录精确 commit SHA；只读元数据或文档摘要记录其 URL、观测或快照标识，不伪称完成了源码分析。

每条重要结论必须区分 `fact`、`inference`、`opinion`，附来源、理由和置信度。记录模型、Prompt 版本、生成时间和被分析输入。缺证据的判断留在提案中待核实，不进入正式摘要。Agent 自报置信度不能当成经过校准的概率。

研究覆盖项目解决的问题、核心架构、关键技术、替代方案、适用场景、工程局限及源码学习价值。复核侧重技术实质与营销差异、贡献者与维护者分布、Issue 响应、测试、Release 和 License 风险。无法获得的证据应标记未知，不据此断言项目低质或已经停止维护。

### 14.3 评分与私人上下文

评分只用于建议和排序，不改变 `research_stage`、`decision` 或 `tracking`。维度统一为 0 到 10，总分规范化为 0 到 100：

```text
RadarScore = 10 × (
  Innovation × 0.25
  + Engineering × 0.20
  + Relevance × 0.25
  + Activity × 0.15
  + LearningValue × 0.15
)
```

每个维度有理由、证据、置信度和版本。总分是派生值，不写回项目事实。公式、输入分析版本和 `score_version` 一并展示，避免将不同规则下的分数直接比较。缺少必要维度时总分缺失，不补零或自动重分配权重。

`Relevance` 按自研项目或关注上下文分别评估，由人填写或确认。私人上下文保存在 `data/contexts/<id>.yaml`，包含目标、当前技术问题与优先级；项目可分别关联 OpenScope、Everglow 等上下文，但这些仅是原稿中的用途示例，不代表已有数据。

全局相关度根据当前活跃上下文及人工确认的聚合配置派生，策略带版本。没有可用上下文或未选择聚合规则时，不给出全局相关度。上下文变化不能静默改写过去报告。

### 14.4 独立关系边

关系保存在 `data/relations/<id>.yaml`，包含唯一 ID、起点、终点、类型、方向、理由和证据。端点带类型，明确指向项目或私人上下文，避免同名 ID 混淆。

一条关系只保存一次，类型来自受控词表。对称关系由读取或展示层展开，不在双方项目文件各存一份。Agent 只能建议关系，人工合并后才生效。引用检查阻止悬空端点；项目合并通过显式迁移及重定向处理关系。关系存储不要求引入图数据库。

<a id="implementation"></a>

## 15. 实现结构与依赖边界

V0.1 采用 Python 包、GitHub API、GitHub Actions、YAML、JSONL、Markdown 和 Jinja2。LLM SDK 按可选增强引入。具体版本在实现时锁定并验证，不在文档中假定当前可用版本。

领域规则放在统一 Python 包内，CLI 和 GitHub Actions 只负责输入、调用与退出状态。ID、去重、Schema、调度和错误处理共享实现，不分散在各个独立脚本中。

```text
GitHub Actions / 本地 CLI
  → 应用流程
  → 领域规则与契约
  → GitHub Provider / Git 数据访问 / 生成器
```

外部字段在适配层转换为内部结构。V0.1 仅有 GitHub Provider，保留 `resolve`、`fetch_repository`、`fetch_observation` 等窄接口；`fetch_changes` 在变化分析阶段加入。领域层不依赖 REST 或 GraphQL 响应格式。

不建立动态插件发现、加载或跨来源聚合框架。第二个真实 Provider 出现后再验证接口差异。

规划中的 CLI：

| 命令 | 作用 | 阶段 |
|---|---|---|
| `open-radar ingest` | 授权收录、去重和提案 | V0.1 |
| `open-radar collect` | 到期采集、手动刷新及重试 | V0.1 |
| `open-radar generate` | 重建 README | V0.1 |
| `open-radar validate` | Schema 和全局一致性检查 | V0.1 |
| `open-radar detect-changes` | 生成标准变化事件 | 后续 |

### 15.1 目标目录

以下树展示 V0.1 目标布局，并非当前文件清单。根目录 `schemas/` 是契约定义的唯一来源，包内 `contracts/` 负责加载与跨文件验证。

```text
open-radar/
├── README.md
├── open-radar-project-structure.md
├── pyproject.toml
├── radar.yaml
├── src/open_radar/
│   ├── domain/
│   ├── contracts/
│   ├── providers/github/
│   ├── workflows/
│   ├── storage/
│   ├── generation/
│   └── cli.py
├── schemas/
│   ├── project.v1.json
│   ├── observation.v1.json
│   ├── run-manifest.v1.json
│   └── taxonomy.v1.json
├── data/
│   ├── projects/
│   ├── observations/github/
│   ├── runs/
│   └── taxonomy/
├── templates/
│   └── README.md.j2
├── tests/
│   ├── fixtures/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── tasks/
│   └── todo.md
└── .github/
    ├── ISSUE_TEMPLATE/
    └── workflows/
```

`radar.yaml` 保存人工确认的运行策略、授权和预算配置，不保存密钥。具体预算值、容量和时区策略在上线前确认。时序数据统一使用 UTC，界面可以转换显示。

可选增强增加提案 Schema 和 `prompts/`。后续研究、报告、上下文、关系及变化事件分别使用 `research/`、`reports/`、`data/contexts/`、`data/relations/`、`data/change-events/`，并补齐对应契约。

引擎代码与私人数据放在同一个私有仓库，但通过数据目录和领域接口隔离。包内不硬编码个人项目或私人 Prompt 配置，测试使用匿名 Fixture。将来开源引擎时应单独抽取通用代码、契约和模板，不连同私人 Git 历史直接公开。

<a id="acceptance"></a>

## 16. 验收与实施顺序

V0.1 必须证明完整收录与采集流程可重复运行，并证明失败和权限边界符合设计。本文没有宣布任何产品测试通过。

| 验收项 | 必须提供的证据 |
|---|---|
| 完整流程 | 在授权的受控测试仓库验证 Issue、收录 PR、人工合并、观测发布和 README |
| 幂等性 | 重复事件、并发重复提交及中断重试不产生重复项目、收录 PR 或观测 |
| 故障恢复 | API 限流、超时、部分失败可安全恢复；LLM 失败不阻塞基础收录 |
| 权限边界 | 机器发布无法绕过检查改写人工知识；第三方指令、脚本和伪造产物不被执行或信任 |
| 数据一致性 | Schema、唯一性、引用、月分区、纠正链和未知值语义全部通过 |
| 确定性生成 | 相同输入生成相同 README，CI 重建无差异 |

离线测试使用固定、匿名 Fixture，默认不访问网络或消耗 LLM 预算。真实 API 身份解析、仓库迁移和权限行为由显式启用的集成测试验证。测试仓库、令牌与外部写入须在实施时获得明确授权；本次文档确认不代替操作授权。

实施顺序为：冻结最小契约，打通无 LLM 的基础收录，再实现观测、双通道发布及 README，最后执行上述验收。可选 LLM 在这些机制稳定后接入。后续能力分别立项，不因目录或接口已预留就提前纳入首版。

部署前还需确认运行容量、采集窗口、API 与 LLM 预算、Artifact 保留期、签名机制以及 GitHub 保护规则的可用性。它们是必须验证的部署参数，不是已测得的系统能力。

### V0.1.1 实施状态（2026-09-04）

当前实现已覆盖本地、可离线验证的关键边界：Webhook 投递持久化重放去重，按操作者与接收仓库的速率/预算预留，收录事务与 `pr_creating` 恢复状态，确定性收录 PR 计划，人工合并对账后的项目落盘，以及 observation-only Publisher 的路径、Schema、追加记录和生成 README 检查。受控 E2E 使用临时 Git 仓库和固定 Fixture，不访问网络。

真实 GitHub Issue/PR 写入、分支保护、来源签名和令牌权限仍需在明确授权的测试仓库中通过显式集成测试验证；离线 PR 客户端不会被当作远端写入的替代证据。

### V0.2.2 可用性硬化状态（2026-09-05）

本轮补齐本地运行门禁：生产模式不再默认注入离线 PR fake；运行清单按 `run_id` 幂等追加，`validate` 默认只读；项目、taxonomy 和上下文的损坏 YAML 在 CLI 边界转换为稳定错误；分析与评分命令拒绝未知项目；wheel 携带 README 模板运行时资源。真实远程适配器、分支保护、签名及受控仓库 E2E 仍未宣称完成。

V0.2.3 进一步让关联仓库按各自观测时间独立调度采集；单仓库失败不会丢弃同一项目其他仓库的成功观测。

V0.2.4 增加按项目范围手动刷新采集的入口，并只读取目标项目 YAML；范围不会改变既有到期调度、幂等写入或错误隔离规则。

V0.2.5 由存储层校验项目 YAML 的 `id` 与文件名一致，并在定向读取前拒绝非 slug 项目 ID，避免项目身份漂移与路径越界。
