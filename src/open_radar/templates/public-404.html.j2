<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Open Radar — not found</title>
  <style>
    :root { color-scheme: dark; --canvas: oklch(0.17 0.014 62); --text: oklch(0.91 0.018 82); --muted: oklch(0.71 0.022 76); --accent: oklch(0.78 0.13 76); }
    body { display: grid; min-height: 100vh; margin: 0; place-items: center; color: var(--text); background: var(--canvas); font-family: ui-sans-serif, system-ui, sans-serif; }
    main { width: min(38rem, calc(100% - 2rem)); }
    p { color: var(--muted); line-height: 1.6; }
    a { color: var(--accent); text-underline-offset: 0.22em; }
    h1 { margin: 0; font-family: Iowan Old Style, Palatino Linotype, Book Antiqua, Palatino, serif; font-size: clamp(3rem, 12vw, 7rem); letter-spacing: -0.045em; line-height: 0.9; }
  </style>
</head>
<body><main><p>Open Radar / 404</p><h1>This page is not in the catalog.</h1><p>Return to the <a href="{{ home_url }}">public project ledger</a>.</p></main></body>
</html>
